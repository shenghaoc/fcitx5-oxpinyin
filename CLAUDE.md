# CLAUDE.md

Read [AGENTS.md](AGENTS.md) first — it is the authoritative working-rules
file for this repository (engine API contract, data resolution, phases/STOP
gates, build/test commands, commit identity, and the hard safety rule). This
file exists only so Claude Code finds the same entry point; the two must not
drift — change AGENTS.md, not this pointer.
